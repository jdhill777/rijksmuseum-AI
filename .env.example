# Anthropic API Key - Required for Claude API calls
# Get your key from: https://console.anthropic.com/
# IMPORTANT: Rotate this key regularly and never commit it to version control!
ANTHROPIC_API_KEY=your_api_key_here

# Rijksmuseum API Key - Required for artwork searches
# Get your key from: https://data.rijksmuseum.nl/object-metadata/api/
# IMPORTANT: Protect this key and never expose it in client-side code!
RIJKSMUSEUM_API_KEY=your_rijksmuseum_api_key_here

# MCP Server Configuration - Required for enhanced search capabilities
# This should point to your running Rijksmuseum MCP server
# Default is http://localhost:3003 if running locally
MCP_SERVER_URL=http://localhost:3003

# Server configuration
# PORT: You can change this to any port number you prefer (3002, 8080, etc.)
PORT=3002
# HOST: Use 0.0.0.0 for all network access, 127.0.0.1 for local-only
HOST=0.0.0.0
# ALLOWED_ORIGINS: Security setting for cross-domain access (can be left empty for personal use)
ALLOWED_ORIGINS=
# HOSTNAME: Your custom domain name if hosting behind a proxy (e.g., 'example.com')
HOSTNAME=
